Migrations generated by `pnpm -C server db:generate` (drizzle-kit). Existing numbered files are protected by
`scripts/hooks/guard_protected.sh` — never edit one; generate a new migration. First real schema lands in T-3.1.
