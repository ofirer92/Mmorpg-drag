# XP curve. Read by packages/shared-rules (xp.ts) and validate_balance.py.
# Formula: xp_for_level(L) = base * (L-1)^exp + linear*(L-1), rounded down; level 1 = 0.
# Targets (WORKPLAN §10): level 10 in ~15 min play, level 30 ≈ 12 hours.
version: 1
max_level: 30
base: 50
exponent: 2.2
linear: 40
